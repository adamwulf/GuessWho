<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- The CLI/MCP App Group id, derived from the ONE shared per-channel
	     build var (Config/CLIAppGroup-*.xcconfig, INV-4). This plist is
	     embedded into the bare Mach-O's __TEXT,__info_plist section
	     (CREATE_INFOPLIST_SECTION_IN_BINARY), so the helper reads the id via
	     Bundle.main at runtime and never carries a hardcoded group string. -->
	<key>GuessWhoCLIAppGroup</key>
	<string>$(GUESSWHO_CLI_APP_GROUP)</string>
</dict>
</plist>
