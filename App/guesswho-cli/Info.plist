<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- The CLI/MCP App Group id is NOT carried here. It used to live in this
	     plist and be read at runtime via Bundle.main, but under the
	     /usr/local/bin symlink launch Bundle.main mis-resolves and the key
	     came back nil. The id is now a compiled-in constant generated from
	     $(GUESSWHO_CLI_APP_GROUP) — see guesswho-cli/BuildSettings.swift and
	     the "Generate BuildSettings.swift" run-script phase. This plist stays
	     for the CFBundle basics (version, identifier) embedded into the bare
	     Mach-O's __TEXT,__info_plist section (CREATE_INFOPLIST_SECTION_IN_BINARY). -->
</dict>
</plist>
